<resources>
    <!-- Base application theme — Compose app, AppCompat base only. -->
    <style name="Theme.Perfkit" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
