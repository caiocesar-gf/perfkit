<resources>
    <!-- Night override — Compose handles dark mode internally. -->
    <style name="Theme.Perfkit" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
