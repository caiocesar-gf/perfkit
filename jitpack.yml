jdk:
  - openjdk17

before_install:
  # JitPack environments do not ship local.properties.
  - echo sdk.dir=$ANDROID_HOME > local.properties

build:
  # Publish all four SDK library modules in a single Gradle invocation.
  # The :app module is an application, not a library — skip it.
  - ./gradlew :sdk-api:publishReleasePublicationToMavenLocal
      :sdk-core:publishReleasePublicationToMavenLocal
      :sdk-strictmode:publishReleasePublicationToMavenLocal
      :sdk-debug-ui:publishReleasePublicationToMavenLocal
